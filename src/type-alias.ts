/**
 * type-alias 类型别名
 */

type NameString = string;

const isNameS: NameString = `van`;

type UserType = {
  name: string;
  age: number;
  male: boolean;
};

const isMeType: UserType = {
  name: `van`,
  age: 18,
  male: false,
};
